1)Какие значения могут быть у первого бита в поле Flags IP-пакета?
    1 *
    0 *
    1,0 *
    любое значение
    должен быть всегда равен нулю (всегда равен нулю нулевой бит Flags т.к. он является зарезервированным)

2)Какой модуль предназначен для оптимизации ответа от веб-сервера/
  Модуль для web-сервера, используемый для повышения скорости работы сайтов путём сокращения времени загрузки сайта в браузере (любая из формулировок будет правильной)
    открытый вопрос (ngx_pagespeed/pagespeed) 

3)(вопрос из сферы назовите имя преподавателя) Какой цвет рубашки у преподавателя в одной из популярнейшей системе управления версиями?
    открытый вопрос (извините если неправильно назвала цвет: розовый)

4)Сопоставить поле IP-пакета и его функцию (для упрощения приводятся не все поля)
    Type of Service (приоритетность пакета и вид критерия выбора маршрута)
    IHL (значение длины заголовка)
    Identification (распознавания фрагментов исходного пакета)
    Header Checksum (определяет целостность пакета)
    Flags(контроль над фрагментации)
    Options (не IP Options, которое является необязательным и используется обычно только при отладке сети) (такого поля нет)
    
5)IP inspection проверяет в IP пакетах следующую информацию:
    Ethernet Header *
    IP Header       *
    Source IP Address
    Total Length
    Protocol
    
6)Чему должен быть равен параметр worker_processes:
    открытый вопрос (4, это число обычно эквивалентно количеству ядер процессора, т.к.Nginx является однопоточным)












